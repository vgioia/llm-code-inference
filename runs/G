```
N = int(input())
reinos = []
for _ in range(N):
    x, y = map(int, input().split())
    reinos.append((x, y))

def distance(x1, y1, x2, y2):
    return abs(x1 - x2) + abs(y1 - y2)

infinitos = set()
for i in range(N):
    for j in range(i+1, N):
        if distance(reinos[i][0], reinos[i][1], reinos[j][0], reinos[j][1]) < float('inf'):
            infinitos.add(min(i+1, j+1))
print(*sorted(infinitos), sep=' ')