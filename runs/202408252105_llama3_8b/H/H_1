```
N, S = map(int, input().split())
dp = [0] * (N + 1)
for i in range(1, N + 1):
    l, r, c = map(int, input().split())
    dp[i] = max(dp[i], dp[max(0, i - 1)] + S * min(r, i) - c)
print(max(dp))
```