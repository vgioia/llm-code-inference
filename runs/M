```
n = int(input())
c = [list(map(int, input().split())) for _ in range(n)]
coerente = True
qtd_remover = -1
for i in range(n):
    for j in range(i+1, n):
        if c[i][j] < c[i][i]:
            coerente = False
            break
    if not coerente:
        break
if coerente:
    qtd_remover = 0
    for i in range(n):
        qtd_remover = max(qtd_remover, len([j for j in range(n) if c[i][j] == c[i][i]]))
print(qtd_remover)
```