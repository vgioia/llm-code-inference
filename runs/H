```
N, S = map(int, input().split())
dp = [0] * (N + 1)
cartas = []
for i in range(N):
    l, r, c = map(int, input().split())
    cartas.append((l, r, c))

for i in range(N + 1):
    dp[i] = max(dp[i - 1], S * min(i, len(cartas)) if i < len(cartas) else 0)

print(max(0, dp[-1]))
```