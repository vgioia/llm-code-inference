```python
import sys
input = sys.stdin.read
from itertools import zip_longest

mod = 998244353
data = input().strip().split()

N = int(data[0])
K = int(data[1])
t = list(map(int, data[2:N+3]))
p = list(map(int, data[N+3:2*N+5]))

q = [0] * (N+1)

for i in range(N+1):
    if i + K >= 0 and i + K <= N:
        q[i] = (q[i] + t[i + K]) % mod
    if i - K >= 0 and i - K <= N:
        q[i] = (q[i] + p[i - K]) % mod

print(' '.join(map(str, q)))
```