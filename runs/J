```
import math

def distance(p1, p2):
    return math.sqrt((p1[0] - p2[0])**2 + (p1[1] - p2[1])**2)

vertices = [list(map(int, input().split())) for _ in range(4)]
N = int(input())
players = [[int(i) for i in input().split()] for _ in range(N)]

max_distance = 0
for vertex in vertices:
    for player in players:
        max_distance = max(max_distance, distance(vertex, player))

print(format(max_distance, '.5f'))
```