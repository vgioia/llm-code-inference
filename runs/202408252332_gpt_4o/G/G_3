```python
import sys
input = sys.stdin.read

data = input().split()
N = int(data[0])
coords = [(int(data[2*i+1]), int(data[2*i+2])) for i in range(N)]

min_x = min(coords, key=lambda t: t[0])[0]
max_x = max(coords, key=lambda t: t[0])[0]
min_y = min(coords, key=lambda t: t[1])[1]
max_y = max(coords, key=lambda t: t[1])[1]

infinite_reigns = set()
for i, (x, y) in enumerate(coords):
    if x == min_x or x == max_x or y == min_y or y == max_y:
        infinite_reigns.add(i + 1)

print(" ".join(map(str, sorted(infinite_reigns))) + "\n")
```