```python
import sys

input = sys.stdin.read
data = input().split()

D = int(data[0])
C = int(data[1])
R = int(data[2])

cansativas = [int(data[i + 3]) for i in range(C)]
revigorantes = [int(data[i + 3 + C]) for i in range(R)]

i, j, atividades_realizadas = 0, 0, 0

while i < C or j < R:
    if i < C and D >= cansativas[i]:
        D -= cansativas[i]
        i += 1
        atividades_realizadas += 1
    elif j < R:
        D += revigorantes[j]
        j += 1
        atividades_realizadas += 1
    else:
        break

print(atividades_realizadas)
```